<metro:MetroWindow x:Class="PlanScoreCard.Views.PluginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:local="clr-namespace:PlanScoreCard.Views.Plugins"
        mc:Ignorable="d"
        Title="PluginView" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <UserControl Grid.Row="0" Content="{Binding PluginConsoleUserControl}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <UserControl Grid.Row="1" Content="{Binding PluginPlotUserControl}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
    </Grid>
</metro:MetroWindow>

