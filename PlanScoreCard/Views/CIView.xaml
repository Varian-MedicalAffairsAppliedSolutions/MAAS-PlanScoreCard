<UserControl x:Class="PlanScoreCard.Views.CIView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlanScoreCard.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <StackPanel>
                <TextBlock Text="Target RX Dose" FontWeight="Bold" Foreground="White"/>
                <TextBox Text="{Binding Dose}" Width="100" Margin="5"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Dose Unit" FontWeight="Bold" Foreground="White"/>
                <ComboBox ItemsSource="{Binding DoseUnits}" SelectedValue="{Binding SelectedDoseUnit}" Margin="5" Width="80"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
