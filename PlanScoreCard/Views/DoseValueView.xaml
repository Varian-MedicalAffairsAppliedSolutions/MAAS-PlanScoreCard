<UserControl x:Class="PlanScoreCard.Views.DoseValueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlanScoreCard.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock FontWeight="Bold" Text="Dose Unit" Foreground="White" HorizontalAlignment="Center"/>
            <ComboBox Width="80" ItemsSource="{Binding DoseUnits}" SelectedValue="{Binding SelectedDoseUnit}"
                      Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
